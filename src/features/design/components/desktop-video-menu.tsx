import * as React from 'react';import { designConfig, DesignConfigKey } from '@/config/design-config';import { cn } from '@/utils/cn';export type VideoMenuProps = {    activeVideo: string | null;    onPlayVideo: (id: string) => void;};type NavigationButtonProps = {    id: string;    activeVideo: string | null;    variant?: boolean | false;    onClick: () => void;    children?: React.ReactNode;};const NavigationButton: React.FC<NavigationButtonProps> = ({    id,    activeVideo,    variant,    onClick,    children,}) => {    return (        <button onClick={onClick} className={`group`}>            <p                className={cn(                    'font-secondaryLight text-base font-light uppercase text-white/70 group-hover:text-white group-hover:font-bold',                    variant ? `border-r pr-8` : ``,                    activeVideo === id ? 'font-bold text-white' : '',                )}            >                {children}            </p>        </button>    );};export const DesktopVideoMenu: React.FC<VideoMenuProps> = ({    activeVideo,    onPlayVideo,}) => {    return (        <div            className={`absolute bottom-0 z-10 hidden w-full pb-[5vw] md:flex`}        >            <div                className={`box-border flex w-full flex-row justify-center gap-8`}            >                {Object.keys(designConfig).map((key) => (                    <NavigationButton                        key={key}                        activeVideo={activeVideo}                        variant={key === 'tour'}                        id={key}                        onClick={() => onPlayVideo(key)}                    >                        {designConfig[key as DesignConfigKey].title}                    </NavigationButton>                ))}            </div>        </div>    );};