import React from 'react';type SplitScreenLayoutProps = {    pageRef: React.LegacyRef<HTMLElement>;    leftContent: React.ReactNode;    rightContent: React.ReactNode;};export const SplitScreenLayout: React.FC<SplitScreenLayoutProps> = ({    pageRef,    leftContent,    rightContent,}) => {    return (        <section            className={`flex min-h-screen w-screen items-center justify-center bg-background-secondary py-16`}            ref={pageRef}        >            <div                className={`box-border flex h-[50.875rem] w-[70rem] flex-row gap-32 overflow-hidden`}            >                <div className={`size-full flex-1`}>{leftContent}</div>                <div className={`h-full flex-1 `}>{rightContent}</div>            </div>        </section>    );};