import { cva, type VariantProps } from 'class-variance-authority';import * as React from 'react';import { Paragraph, ParagraphProps } from '@/components/ui/paragraph';import { Subtitle, SubtitleProps } from '@/components/ui/subtitle';import { Title, TitleProps } from '@/components/ui/title';import { cn } from '@/utils/cn';const flexVariants = cva('', {    variants: {        variant: {            col: 'flex flex-col items-start justify-start',            row: 'flex h-[181px] flex-row gap-8',        },    },    defaultVariants: {        variant: 'col',    },});const paragraphIndentVariants = cva('', {    variants: {        indentVariant: {            default:                'inline-flex items-center justify-center gap-2 border-l border-[#84936e] pb-8 pl-8',            flexEnd:                ' flex w-[450px] items-end gap-2 border-l border-[#84936e] pl-8',            dark: 'inline-flex flex-col items-start justify-center gap-8 border-l border-[#162425] pb-8 pl-8',        },    },    defaultVariants: {        indentVariant: 'default',    },});export type SectionTitleProps = VariantProps<typeof flexVariants> &    VariantProps<typeof paragraphIndentVariants> &    SubtitleProps &    TitleProps &    ParagraphProps & {        subtitle?: string;        title?: string;        paragraph?: string;        children?: React.ReactNode;    };export const SectionTitle: React.FC<SectionTitleProps> = ({    variant,    size,    indentVariant,    subtitleVariant,    titleVariant,    paragraphVariant,    title,    subtitle,    paragraph,    children,}) => {    return (        <div className={cn(flexVariants({ variant }))}>            <div>                {subtitle && (                    <Subtitle subtitleVariant={subtitleVariant}>                        {subtitle}                    </Subtitle>                )}                {title && (                    <Title titleVariant={titleVariant} size={size}>                        {title}                    </Title>                )}            </div>            {paragraph && (                <div className={cn(paragraphIndentVariants({ indentVariant }))}>                    <Paragraph paragraphVariant={paragraphVariant}>                        {paragraph}                    </Paragraph>                    {children}                </div>            )}        </div>    );};