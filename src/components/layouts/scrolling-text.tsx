import * as React from 'react';import { InfiniteScroll } from '@/components/ui/infitine-scroll';type SplitHeadingProps = {    children: string;};const SplitHeading: React.FC<SplitHeadingProps> = ({ children }) => {    return (        <div className={`flex flex-row`}>            {children.split('').map((item, idx) => (                <h1                    className={`text-center font-primaryLight text-[300px] font-light uppercase leading-[300px] text-[#84936e]/50`}                    key={idx}                >                    {item === ' ' ? <>&nbsp;</> : item}                </h1>            ))}        </div>    );};export const ScrollingText = () => {    return (        <div            className={`flex h-[500px] w-full flex-row items-center justify-center overflow-hidden bg-background-secondary`}        >            <InfiniteScroll>                <SplitHeading>140 WILLIAM STREET • </SplitHeading>            </InfiniteScroll>        </div>    );};