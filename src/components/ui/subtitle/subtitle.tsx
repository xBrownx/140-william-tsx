import { cva, type VariantProps } from 'class-variance-authority';import * as React from 'react';import { cn } from '@/utils/cn';export const subtitleVariants = cva(    'pb-2 font-secondaryRegular text-base font-normal uppercase',    {        variants: {            subtitleVariant: {                default: 'text-[#84936e]',                dark: 'text-[#164a49]',            },        },        defaultVariants: {            subtitleVariant: 'default',        },    },);export type SubtitleProps = VariantProps<typeof subtitleVariants> & {    children?: React.ReactNode;};export const Subtitle: React.FC<SubtitleProps> = ({    subtitleVariant,    children,}) => {    return (        <h3 className={cn(subtitleVariants({ subtitleVariant }))}>            {children}        </h3>    );};