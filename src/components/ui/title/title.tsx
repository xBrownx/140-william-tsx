import { cva, type VariantProps } from 'class-variance-authority';import * as React from 'react';import { cn } from '@/utils/cn';export const titleVariants = cva(    'whitespace-pre-wrap font-primaryLight text-[64px] font-light leading-[76.80px]',    {        variants: {            titleVariant: {                default: 'pb-8 text-[#164a49]',                light: 'pb-8 text-[#ffffff]',                dark: 'pb-8 text-[#162425]',            },            size: {                default: 'w-fit',                row: 'w-[640px]',            },        },        defaultVariants: {            titleVariant: 'default',            size: 'default',        },    },);export type TitleProps = VariantProps<typeof titleVariants> & {    children?: React.ReactNode;};export const Title: React.FC<TitleProps> = ({    titleVariant,    size,    children,}) => {    return (        <h1 className={cn(titleVariants({ titleVariant, size }))}>            {children}        </h1>    );};