import * as React from 'react';import { amenitiesAssets as ASSETS } from '@/assets';import { SectionTitle } from '@/components/layouts';import Video from '@/components/ui/video/video';import { useNavigationContext } from '@/context/navigation-context';import { AmenitiesList } from '@/features/amenities/components';export const AmenitiesRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    return (        <section            className={`flex w-screen items-center justify-center bg-background-secondary pt-16 md:min-h-screen md:py-16`}            ref={pageRefs.amenities}        >            <div                className={`box-border flex w-full flex-col gap-8 overflow-hidden md:h-[50.875rem] md:w-[70rem] md:flex-row md:gap-16`}            >                <div                    className={`box-border size-full w-full flex-1 px-4 md:px-0`}                >                    <SectionTitle                        titleVariant={'light'}                        subtitle={'AMENITIES'}                        title={'Elite Services\nand Amenities'}                    />                    <AmenitiesList />                </div>                <div                    className={`box-border size-full flex-1 overflow-hidden md:rounded-lg`}                >                    <Video                        options={{                            src: ASSETS.vid['hero-video'].src,                            loop: true,                            autoplay: true,                            muted: true,                        }}                    />                </div>            </div>        </section>    );};