import * as React from 'react';import { historyAssets as ASSETS } from '@/assets';import { SectionTitle } from '@/components/layouts';import { useNavigationContext } from '@/context/navigation-context';import { historyConst as CONST } from 'src/constants';import Video from '@/components/ui/video/video';export const HistoryRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    return (        <section            ref={pageRefs.history}            className={`h-fit w-screen bg-background-primary`}        >            <div                className={`box-border flex size-full h-fit flex-row items-center justify-center gap-32 overflow-hidden px-40 pb-10 pt-32`}            >                <SectionTitle                    variant={'row'}                    size={'row'}                    indentVariant={'flexEnd'}                    subtitle={CONST.subtitle}                    title={CONST.title}                    paragraph={CONST.paragraph}                />            </div>            <div                className={`box-border h-fit overflow-hidden rounded-lg px-16 pb-32`}            >                <div className={`overflow-hidden rounded-lg`}>                    <Video                        options={{                            src: ASSETS.vid['hero-video'].src,                            controls: true,                            autoplay: false,                            poster: ASSETS.img['video-poster'].src,                        }}                    />                </div>            </div>        </section>    );};