import { landingAssets } from '@/assets';import Video from '@/components/ui/video/video';import { useNavigationContext } from '@/context/navigation-context';import { landingConst } from 'src/constants';export const LandingRoute = () => {    const { pageRefs } = useNavigationContext();    const bgVideo = landingAssets.vid['background-video'];    return (        <section ref={pageRefs.landing}>            <div className="flex h-screen max-h-screen w-screen justify-center overflow-hidden px-0 align-middle">                <Video                    options={{                        src: bgVideo.src,                        controls: false,                        autoplay: true,                        loop: true,                    }}                >                    <div className="absolute z-10 flex size-full justify-center align-middle">                        <p className="h-min">{landingConst.title[0].title}</p>                    </div>                </Video>            </div>        </section>    );};