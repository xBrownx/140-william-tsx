import * as React from 'react';import { availabilityAssets as ASSETS } from '@/assets';import { SectionTitle } from '@/components/layouts';import { useNavigationContext } from '@/context/navigation-context';import { availabilityConst as CONST } from 'src/constants';import { Paragraph } from '@/components/ui/paragraph';export const AvailabilityRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    return (        <section className={`h-fit w-screen`} ref={pageRefs.availability}>            <div                className={`relative h-0 w-full overflow-hidden bg-availability bg-contain bg-no-repeat pt-[143.88%]`}            >                <div                    className={`absolute inset-0 box-border flex size-full flex-row gap-32 overflow-hidden px-40 py-32`}                >                    <SectionTitle                        subtitleVariant={'dark'}                        indentVariant={'dark'}                        titleVariant={'dark'}                        paragraphVariant={'dark'}                        subtitle={CONST.subtitle}                        title={CONST.title}                        paragraph={CONST.paragraph}                    >                        <Paragraph paragraphVariant={'dark'}>                            NABERS Energy Rating                        </Paragraph>                        <Paragraph paragraphVariant={'dark'}>                            NABERS Water Rating                        </Paragraph>                    </SectionTitle>                </div>            </div>        </section>    );};