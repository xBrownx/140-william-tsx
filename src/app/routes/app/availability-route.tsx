import * as React from 'react';import { useNavigationContext } from '@/context/navigation-context';import { AvailabilityDesktop } from '@/features/availability/components/availability-desktop';import { AvailabilityMobile } from '@/features/availability/components/availability-mobile';import { useMobile } from '@/hooks/use-mobile';export const AvailabilityRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    const isMobile = useMobile();    return (        <section className={`h-fit w-screen`} ref={pageRefs.availability}>            {isMobile ? <AvailabilityMobile /> : <AvailabilityDesktop />}        </section>    );};