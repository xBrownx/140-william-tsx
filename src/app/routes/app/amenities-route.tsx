import * as React from 'react';import { amenitiesAssets as ASSETS } from '@/assets';import { SectionTitle } from '@/components/layouts';import Video from '@/components/ui/video/video';import { useNavigationContext } from '@/context/navigation-context';import { amenitiesConst as CONST } from 'src/constants';const listItems = CONST.menuItems;const itemIcons = ASSETS.icon;type Key = keyof typeof listItems;type IconKey = keyof typeof itemIcons;const AmenitiesList = () => {    return (        <div className={`mt-4 border-l border-[#84936E] pl-8 md:mt-0`}>            <ul className={`flex list-none flex-col justify-between gap-8`}>                {Object.keys(listItems).map((key) => (                    <li                        key={key}                        className={`flex flex-row items-center gap-4 opacity-70`}                    >                        <img                            src={itemIcons[key as IconKey].src}                            alt={listItems[key as Key].title}                        />                        <p                            className={`font-secondaryLight text-lg font-normal leading-[27px] tracking-[0.031rem] text-white/70`}                        >                            {listItems[key as Key].title}                        </p>                    </li>                ))}            </ul>        </div>    );};export const AmenitiesRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    return (        <section            className={`flex w-screen items-center justify-center bg-background-secondary pt-16 md:min-h-screen md:py-16`}            ref={pageRefs.amenities}        >            <div                className={`box-border flex w-full flex-col gap-8 overflow-hidden md:h-[50.875rem] md:w-[70rem] md:flex-row md:gap-16`}            >                <div                    className={`box-border size-full w-full flex-1 px-4 md:px-0`}                >                    <SectionTitle                        titleVariant={'light'}                        subtitle={CONST.subtitle}                        title={CONST.title}                    />                    <AmenitiesList />                </div>                <div                    className={`box-border size-full flex-1 overflow-hidden rounded-lg`}                >                    <Video                        options={{                            src: ASSETS.vid['hero-video'].src,                            loop: true,                            autoplay: true,                            muted: true,                        }}                    />                </div>            </div>        </section>    );};