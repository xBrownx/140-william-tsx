import * as React from 'react';import { useNavigationContext } from '@/context/navigation-context';import { enquireConst as CONST } from 'src/constants';export const EnquireRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    return (        <section className={`h-screen w-screen`} ref={pageRefs.enquire}>            <div                className={`box-border flex size-full flex-row gap-32 overflow-hidden px-40 py-32`}            ></div>        </section>    );};