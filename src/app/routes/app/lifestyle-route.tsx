import * as React from 'react';import { lifestyleAssets as ASSETS } from '@/assets';import { SectionTitle } from '@/components/layouts';import { InfiniteScroll } from '@/components/ui/infitine-scroll';import { lifestyleConst as CONST } from '@/constants';import { useMobile } from '@/hooks/use-mobile';import { cn } from '@/utils/cn';const ScrollingImages = () => {    return (        <InfiniteScroll>            <div className="flex h-[300px] w-max gap-8 pr-8">                {ASSETS.img.carousel.map((image, idx) => (                    <img                        key={idx}                        src={image.src}                        alt={image.alt}                        className={`h-full w-auto rounded-lg object-contain`}                    />                ))}            </div>        </InfiniteScroll>    );};export const LifestyleRoute: React.FC = () => {    const isMobile = useMobile();    return (        <section            className={cn(                `box-border h-screen w-screen bg-background-primary`,                isMobile ? '' : '',            )}        >            <div                className={cn(                    `box-border flex size-fit gap-32 overflow-hidden px-40 pb-8 pt-32`,                    isMobile ? 'px-4 pt-16 pb-16' : '',                )}            >                <SectionTitle                    variant={isMobile ? 'mobile' : 'col'}                    subtitleVariant={'default'}                    indentVariant={'default'}                    subtitle={CONST.subtitle}                    title={CONST.title}                    paragraph={CONST.paragraph}                />            </div>            <ScrollingImages />        </section>    );};