import * as React from 'react';import { lifestyleAssets as ASSETS } from '@/assets';import { SectionTitle } from '@/components/layouts';import { InfiniteScroll } from '@/components/ui/infitine-scroll';import { lifestyleConst as CONST } from 'src/constants';const ScrollingImages = () => {    return (        <InfiniteScroll>            <div className="flex h-[300px] w-max gap-8 pr-8">                {ASSETS.img.carousel.map((image, idx) => (                    <img                        key={idx}                        src={image.src}                        alt={image.alt}                        className={`h-full w-auto rounded-lg object-contain`}                    />                ))}            </div>        </InfiniteScroll>    );};export const LifestyleRoute: React.FC = () => {    return (        <section className={`h-screen w-screen bg-background-primary`}>            <div                className={`box-border flex size-fit gap-32 overflow-hidden px-40 pt-32 pb-8`}            >                <SectionTitle                    subtitleVariant={'default'}                    indentVariant={'default'}                    subtitle={CONST.subtitle}                    title={CONST.title}                    paragraph={CONST.paragraph}                />            </div>            <ScrollingImages />        </section>    );};