import * as React from 'react';import { homeAssets as ASSETS } from '@/assets';import { SplitScreenLayout } from '@/components/layouts';import { ParallaxImage } from '@/components/ui/parallax';import { useNavigationContext } from '@/context/navigation-context';import { useMobile } from '@/hooks/use-mobile';import { cn } from '@/utils/cn';import { homeConst as CONST } from 'src/constants';const RightContent = () => {    const { navigateToPage } = useNavigationContext();    const isMobile = useMobile();    return (        <div            className={cn(                `flex flex-col gap-8 md:px-0 justify-center h-full`,                isMobile ? 'w-auto px-4' : '',            )}        >            <h2                className={cn(                    `font-secondaryRegular md:text-base font-normal leading-[1.2rem] text-font-primary-full`,                    isMobile                        ? 'text-[#164a49] text-base font-bold uppercase leading-tight'                        : '',                )}            >                {CONST.title}            </h2>            <p                className={cn(                    `font-secondaryRegular text-4xl font-normal text-font-primary-light tracking-[0.031rem] md:w-[110%]`,                    isMobile                        ? 'text-[#164a49]/70 text-2xl font-normal leading-7 whitespace-normal'                        : '',                )}            >                {CONST.paragraph}            </p>            <button                onClick={() => navigateToPage('availability')}                className={cn(                    `group relative flex w-fit overflow-hidden rounded-lg border border-background-secondary md:py-4 md:px-8 shadow-none`,                    'py-2 px-4',                    `md:after:absolute md:after:inset-x-0 md:after:-top-full md:after:bottom-full md:after:-z-0 md:after:block md:after:bg-button-primary md:after:px-2 md:after:py-6 md:after:transition-all md:after:duration-300 md:after:content-[''] md:hover:after:inset-x-0 md:after:hover:bottom-0 md:hover:after:top-0 `,                )}            >                <p                    className={cn(                        `z-10 font-secondaryRegular text-base font-normal text-font-primary-full transition-all duration-300 md:group-hover:text-white tracking-[0.031rem]`,                        isMobile ? '' : '',                    )}                >                    {CONST.button}                </p>            </button>        </div>    );};export const HomeRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    return (        <SplitScreenLayout            pageRef={pageRefs.home}            leftContent={                <div className={`h-full overflow-hidden px-4 md:p-0`}>                    <ParallaxImage                        src={ASSETS.img.hero.src}                        alt={ASSETS.img.hero.alt}                        className={`size-full rounded-lg `}                    />                </div>            }            rightContent={<RightContent />}        />    );};