import * as React from 'react';import { homeAssets as ASSETS } from '@/assets';import { ParallaxImage } from '@/components/ui/parallax-image';import { useNavigationContext } from '@/context/navigation-context';import { useMobile } from '@/hooks/use-mobile';import { cn } from '@/utils/cn';import { homeConst as CONST } from 'src/constants';export const HomeRoute: React.FC = () => {    const { pageRefs, navigateToPage } = useNavigationContext();    const isMobile = useMobile();    return (        <section            ref={pageRefs.home}            className={cn(                `box-border flex min-h-screen w-screen items-center justify-center overflow-hidden bg-background-primary`,                isMobile ? 'h-fit' : '',            )}        >            <div                className={cn(                    `box-border flex h-[50.875rem] w-[70rem] md:gap-16 overflow-hidden md:flex-row md:p-0`,                    `flex-col px-0 pt-16 pb-8 gap-8`,                )}            >                <div                    className={`order-2 box-border flex flex-1 px-4 md:order-1 md:px-0`}                >                    <ParallaxImage                        src={ASSETS.img.hero.src}                        alt={ASSETS.img.hero.alt}                        className={`size-full rounded-lg`}                    />                </div>                <div                    className={`order-1 flex flex-1 flex-col justify-center md:order-2`}                >                    <div                        className={cn(                            `flex w-[35rem] flex-col gap-8 md:pl-[1.1rem] box-border`,                            isMobile ? 'w-auto px-4' : '',                        )}                    >                        <h2                            className={cn(                                `font-secondaryRegular md:text-base font-normal leading-[1.2rem] text-font-primary-full`,                                isMobile                                    ? 'text-[#164a49] text-base font-bold uppercase leading-tight'                                    : '',                            )}                        >                            {CONST.title}                        </h2>                        <p                            className={cn(                                `font-secondaryRegular text-4xl font-normal text-font-primary-light`,                                isMobile                                    ? 'text-[#164a49]/70 text-2xl font-normal leading-7 whitespace-normal'                                    : '',                            )}                        >                            {CONST.paragraph}                        </p>                        <button                            onClick={() => navigateToPage('availability')}                            className={`group relative flex w-fit overflow-hidden rounded-lg border border-background-secondary px-8 py-4 after:absolute after:inset-x-0 after:-top-full after:bottom-full after:-z-0 after:block after:bg-button-primary after:px-2 after:py-6 after:transition-all after:duration-300 after:content-[''] hover:after:inset-x-0 after:hover:bottom-0 hover:after:top-0`}                        >                            <p                                className={cn(                                    `z-10 font-secondaryRegular text-base font-normal text-font-primary-full transition-all duration-300 group-hover:text-white`,                                    isMobile ? '' : '',                                )}                            >                                {CONST.button}                            </p>                        </button>                    </div>                </div>            </div>        </section>    );};