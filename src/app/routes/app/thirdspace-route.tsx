import * as React from 'react';import { availabilityAssets as ASSETS } from '@/assets';import { SectionTitle } from '@/components/layouts';import { thirdspaceConst as CONST } from 'src/constants';import { SwiperGallery } from '@/components/layouts/swiper-gallery';export const ThirdspaceRoute: React.FC = () => {    return (        <section            className={`box-border h-fit w-screen bg-background-secondary`}        >            <div                className={`box-border flex size-full flex-col overflow-hidden px-40 pt-32`}            >                <SectionTitle                    titleVariant={'light'}                    subtitle={CONST.subtitle}                    title={CONST.title}                />            </div>            <div className={`rounded-lg px-16 pb-32 h-fit box-border`}>                <SwiperGallery images={ASSETS.img['third-space']} />            </div>        </section>    );};