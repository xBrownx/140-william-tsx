import * as React from 'react';import { availabilityAssets as ASSETS } from '@/assets';import { SectionTitle, SwiperGallery } from '@/components/layouts';import { useMobile } from '@/hooks/use-mobile';import { thirdspaceConst as CONST } from 'src/constants';export const ThirdspaceRoute: React.FC = () => {    const isMobile = useMobile();    return (        <section            className={`box-border h-fit w-screen bg-background-secondary`}        >            <div                className={`box-border flex size-full flex-col overflow-hidden pt-16 md:px-40 md:pt-32`}            >                <SectionTitle                    titleVariant={'light'}                    variant={isMobile ? 'mobile' : 'col'}                    subtitle={CONST.subtitle}                    title={CONST.title}                />            </div>            <div className={`box-border h-fit rounded-lg pb-32`}>                <SwiperGallery images={ASSETS.img['third-space']} />            </div>        </section>    );};