import * as React from 'react';import { galleryAssets as ASSETS } from '@/assets';export const GalleryRoute: React.FC = () => {    return (        <section className={`h-screen w-screen`}>            <div                className={`box-border flex size-full flex-row gap-32 overflow-hidden px-40 py-32 bg-background-primary`}            ></div>        </section>    );};