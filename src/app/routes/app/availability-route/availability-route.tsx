import * as React from 'react';import { LevelButtons } from '@/app/routes/app/availability-route/components/level-buttons';import { LevelVectors } from '@/app/routes/app/availability-route/components/level-vectors';import { Modal } from '@/app/routes/app/availability-route/components/modal/modal';import { availabilityAssets as ASSETS } from '@/assets';import { SectionTitle } from '@/components/layouts';import { Paragraph } from '@/components/ui/paragraph';import { availabilityConst as CONST } from '@/constants';import { useNavigationContext } from '@/context/navigation-context';const Overlay = () => {    return (        <>            <div                className={`absolute top-0 box-border size-full mix-blend-normal`}            >                <LevelVectors />                <LevelButtons />                <Modal />            </div>        </>    );};export const AvailabilityRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    return (        <section className={`h-fit w-screen`} ref={pageRefs.availability}>            <div                className={`relative h-0 w-full overflow-hidden bg-availability bg-contain bg-no-repeat pt-[143.88%]`}            >                <Overlay />                <div                    className={`absolute inset-0 box-border flex size-full flex-row gap-32 overflow-hidden px-40 py-32`}                >                    <SectionTitle                        subtitleVariant={'dark'}                        indentVariant={'dark-no-padding'}                        titleVariant={'dark'}                        paragraphVariant={'dark'}                        subtitle={CONST.subtitle}                        title={CONST.title}                        paragraph={CONST.paragraph}                    >                        <div className={'flex w-fit flex-col gap-1'}>                            <img                                className={'w-fit'}                                src={ASSETS.icon['rating-two'].src}                                alt={ASSETS.icon['rating-two'].alt}                            />                            <Paragraph paragraphVariant={'dark'}>                                NABERS Energy Rating                            </Paragraph>                        </div>                        <div className={'flex w-fit flex-col gap-1'}>                            <img                                className={'w-fit'}                                src={ASSETS.icon['rating-one'].src}                                alt={ASSETS.icon['rating-one'].alt}                            />                            <Paragraph paragraphVariant={'dark'}>                                NABERS Water Rating                            </Paragraph>                        </div>                    </SectionTitle>                </div>            </div>        </section>    );};