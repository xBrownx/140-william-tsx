import * as React from 'react';import { AvailabilityDesktop } from '@/app/routes/app/availability-route/components/availability-desktop';import { AvailabilityMobile } from '@/app/routes/app/availability-route/components/availability-mobile';import { useNavigationContext } from '@/context/navigation-context';import { useMobile } from '@/hooks/use-mobile';export const AvailabilityRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    const isMobile = useMobile();    return (        <section className={`h-fit w-screen`} ref={pageRefs.availability}>            {isMobile ? <AvailabilityMobile /> : <AvailabilityDesktop />}        </section>    );};