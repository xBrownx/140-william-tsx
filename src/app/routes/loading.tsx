import * as React from 'react';import logo from '@/assets/large-logo.png';import { fadeInOut, swipeInOutUp } from '@/components/configs';import { useInitialLoadContext } from '@/context/initial-load-context';import { ProgressBar } from 'src/components/ui/progress-bar';export const LoadingScreen: React.FC = () => {    const { isLoadComplete } = useInitialLoadContext();    return (        <div            className={`absolute top-0 z-50 flex h-screen w-screen items-center justify-center bg-background-secondary transition-all duration-1000 ease-out            ${isLoadComplete && swipeInOutUp.hide}`}        >            <div                className={`relative z-50 flex h-[8.5rem] w-[32.6875rem] items-center justify-center`}            >                <img                    src={logo}                    alt={'logo'}                    className={`absolute left-0 top-0 size-full                         ${isLoadComplete ? fadeInOut.show : fadeInOut.hide}`}                />                <ProgressBar />            </div>        </div>    );};